import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Nojin9")
                .font(.title)
                .fontWeight(.semibold)

            Text("C4 MVP")
                .font(.subheadline)
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}

